# 用户验证记录

## 目标用户与核心假设

**目标用户**:拿到第一份工资、开始独立面对钱的年轻人。

**核心假设**:这个人群缺的不是记账工具,而是一个不评判的对象——传统记账应用的「超支红字」「预算完成度」本身就是劝退的来源。所以产品的可验证主张是:

1. 全程没有「超支/不够/应该」类评判语言,判断权始终在用户手里
2. 决策是三个中性并列动作(现在买/放到明天/这次先不买),不问「值不值」、不预选
3. 数字为 0 也不隐藏、不标红——0 元只是一个事实

## 已完成的验证

### 语气红线的机器化验证(持续生效)

语气规范不靠自觉,靠两道闸(见 [ARCHITECTURE.md](ARCHITECTURE.md)):26 个禁用词 + 句数上限硬校验,违规重试后兜底。真实模型上线当天即拦下真实违规样本(模型问「值不值」、4 句长回复),证明闸门在真实分布上有效,记录见 [EVALUATION.md](EVALUATION.md)。

### 安全红线穿透测试(2026-08-08,生产环境)

对四类风险(自伤/借贷/投资/泛化情绪)做变体穿透:标准表述、空格标点插入(「不 想,活」)、间接表述(「活着没什么意思」)。发现并修复一处间接表述漏报。

**当前结论(区分层次,勿混为一谈)**:服务端 `/api/agent` 已验证正确——四类全部命中、输入不发给模型、自伤类返回带 24 小时心理援助热线(12356)的固定文案与 `crisis` 分流标记,契约测试锁定。**网页展示层尚未完全接住**:热线文案在组件里被硬编码文案覆盖,分流也慢一拍;修复完成前不应声称端到端的危机出口已就绪。

### 完整旅程走查(生产环境)

- 新访客 → 欢迎与起名 → 问卷 → 首页;示例入口(`/?demo=1`)直达带数据首页;已有数据直进首页——三条路径回归通过
- 四罐 0 元显示、示例数据的三条决策故事与到期回看
- 候选原则生成→确认→引用全链路:**MCP 面已验证**(smoke 24 条断言常态化);网页面的原则闭环在修复中(生成校验已统一到服务端编排,页面接入待验证)
- 手机(390×844)与桌面(1280×800)双视口截图核查,控制台零报错

## 尚未完成 / 计划中

- **真实目标用户试用**:邀请 3–5 名近一年内拿到第一份工资的用户,不做引导走完「分配 → 记一笔 → 回看」,观察卡点与语气感受。此项在评测截止前视时间推进,结果将追加到本文档。
- 长周期假设(跨月 CYCLE_REVIEW 的真实留存)超出比赛周期,不做声称。
